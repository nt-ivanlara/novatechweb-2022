import { GenericHeading } from "@/interfaces/generic";

export interface AccordionBoxProps extends GenericHeading {
    info_cards: any;
}

export interface AccordionProps extends GenericHeading {
    hero: any;
    icon: any;
}
