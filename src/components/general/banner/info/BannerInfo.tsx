import { GenericHeading } from "../../../../interfaces/generic";

interface Props extends GenericHeading {
    description: string;
}

const BannerInfo = ({ title, subtitle, description }: Props) => {
    return (
        <div>
            
        </div>
    )
};

export default BannerInfo
