export interface TechProps {
    isCarousel: boolean;
    bgColor: string;
    title: string;
    subtitle?: string;
    styles: StyleProps;
}

export interface StyleProps {
    titleColor: string;
    subtitleColor: string;
}
